{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		"rootDir": "./src",
		// should never actually get built into this directory, as entire build is handled by webpack
		"outDir": "./dist/ts",
		"moduleResolution": "bundler",
		"types": [
			"webpack-env",
			"mocha"
		],
		"declaration": true
	}
}
